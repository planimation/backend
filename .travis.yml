language: python
python: "3.6"
services:
  - docker
before_script: pip install docker-compose

script:
  - docker-compose run planimation_server -c "python manage.py flake8"

# install:
#   - pip install -r ./server/requirements.txt

# script: echo "npm test temporarily disabled"